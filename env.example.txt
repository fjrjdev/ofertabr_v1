# Application Settings
PROJECT_NAME=OfertaBR V1
VERSION=0.1.0
API_V1_STR=/api/v1

# Security - CRITICAL: Change SECRET_KEY in production!
# Generate a secure key with: openssl rand -hex 32
SECRET_KEY=09d25e094faa6ca2556c818166b7a9563b93f7099f6f0f4caa6cf63b88e8d3e7
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=10080

# Database Configuration
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_SERVER=db
POSTGRES_PORT=5432
POSTGRES_DB=ofertabr_db

# Redis Configuration
REDIS_HOST=redis
REDIS_PORT=6379

# CORS - Add your frontend URLs here
BACKEND_CORS_ORIGINS=["http://localhost:3000","http://localhost:8000","http://localhost:5173"]

# Email Configuration (Brevo)
# Get your API key from: https://app.brevo.com/settings/keys/api
BREVO_API_KEY=your-brevo-api-key-here
EMAIL_FROM=noreply@ofertabr.com
EMAIL_FROM_NAME=OfertaBR
FRONTEND_URL=http://localhost:3000
API_URL=http://localhost:8000

# N8N Configuration
# Webhooks (Optional)
# Configure webhook URL from N8N workflow for new subscriber notifications
# Example: http://localhost:5678/webhook/new-subscriber
# Leave empty or comment out if not using N8N
N8N_WEBHOOK_NEW_SUBSCRIBER=

# N8N Service Authentication
# Secret key for n8n to authenticate and get JWT tokens automatically
# Generate a secure secret with: openssl rand -hex 32
# This allows n8n workflows to call protected API endpoints without email verification
N8N_SERVICE_SECRET=change-this-to-a-secure-random-string-min-16-chars

